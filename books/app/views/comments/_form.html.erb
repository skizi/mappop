
	<%= form_for(comment) do |f| %>
		<div class="c_mb40 question_id">
			<%= f.label :question_id %>
			<%= f.text_field :question_id %>
		</div>
		<div class="c_mb40 content">
			<%= f.label :content %>
			<%= f.text_area :content %>
		</div>

		<%= f.label :user_id %>
		<%= f.text_field :user_id %>

		<%= f.submit class:"submit_btn0 c_mcenter" %>
		
	<% end %>